import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    flexShrink: 0,
    borderRadius: '50%',
    userSelect: 'none',
  },
}));

export default useStyles;
