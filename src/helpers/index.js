import toPrecision from './to-precision';
import getChartColors from './get-chart-colors';

export const isNumber = (value) => !Number.isNaN(Number(value));

export {
  toPrecision,
  getChartColors,
};
